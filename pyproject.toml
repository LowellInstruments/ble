[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"


[project]
name = "ble"
version = "0.1.4"
description = "BLE library for Lowell Instruments"
dependencies = [
    "bleak~=1.0",
    "humanize",
    "cacheout"
]

[project.scripts]
main_ctd = "ble.main_ctd:main"


