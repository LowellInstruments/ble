[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"


[project]
name = "ble"
version = "0.1.1"
description = "BLE library for Lowell Instruments"
dependencies = [
    "bleak",
    "humanize"
]